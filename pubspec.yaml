name: vorzela_json
description: JSON from the network to Dart models — DateTime, enums, JsonFile, Dio/http helpers. No codegen.
version: 0.2.0
homepage: https://github.com/vorzela/vorzela_json
repository: https://github.com/vorzela/vorzela_json
license: MIT

environment:
  sdk: ^3.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
