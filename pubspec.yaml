name: vorzela_json
description: Write Dart models once — JSON for DateTime, Duration, Uri, BigInt, Enum, bytes. No codegen. Works with Dio and package:http.
version: 0.0.2
homepage: https://github.com/vorzela/vorzela_json
repository: https://github.com/vorzela/vorzela_json
license: MIT

environment:
  sdk: ^3.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
